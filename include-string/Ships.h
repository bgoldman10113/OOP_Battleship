
#include <string>

//A struct that contains all of the information we would need about a ship when it is called in gameplay
const int shipTypes = 5;

struct shipStruct {
    string name;
    int length;
    int shipValue;
};
